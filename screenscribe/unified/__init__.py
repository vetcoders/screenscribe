"""Unified VLM-powered analysis package.

Split out of the former ``screenscribe/unified_analysis.py`` monolith. The
public surface stays importable from ``screenscribe.unified_analysis`` (a thin
facade) so existing import and monkeypatch paths keep working.
"""
